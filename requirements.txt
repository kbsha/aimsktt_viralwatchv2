
pandas
numpy
scikit-learn
matplotlib
seaborn
jupyter
requests
beautifulsoup4
lxml
pdfplumber
joblib
pytest
# Database Integration (Cloud Postgres & Local SQLite support)
SQLAlchemy>=2.0.0
# psycopg2-binary>=2.9.0

# NLP Processing
transformers
torch
sentencepiece
protobuf
tqdm
pyshp
transformers
torch
tqdm


# for dashbord 

# Runtime deps for the API + dashboard. Kept minimal so the Render free web
# service (512 MB RAM) builds fast and stays inside memory.
# The API uses only these + the Python standard library (sqlite3, json, re).
fastapi>=0.110
uvicorn[standard]>=0.29




psycopg[binary]>=3.1.0







